extends ../layout

block content
  h1 New Book

  include table-error
  form(action="/books", method="post")
    include form
    p
    input(type='submit' value='Create New Book')
    p
    a.button(href='/books') Cancel